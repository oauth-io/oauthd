var me = {
    fetch: [

        function(fetched_elts) {
            return '/primarycontact.json';
        }

    ],
    params: {},
    fields: {
        email: 'EmailAddress'
    }
};

module.exports = me;