jf = require 'jsonfile'
Q = require 'q'

module.exports = () ->
	(name, cwd) ->
		launchUpdate = () ->
			defer = Q.defer()


			defer.promise

		checkIfFolderExist = () ->
			return
		checkIfPullNeeded = () ->
			return
		checkIfDependenciesProblems = () ->
			return
		updateGit = () ->
			return
		updatePluginsList = () ->
			return

		launchUpdate()
		